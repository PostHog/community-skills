# Changelog

**Rolling window: last {{ window_days }} days.** One line per change, newest date first. If an entry wants a second sentence, that sentence belongs in `issues/NN`, `ideas/NN`, `environment-notes.md`, or `open-prs.md`, and the line here just points at it. **When you add an entry, delete anything older than the window.** Trimming is part of the write, not a separate chore. Dropped entries are not archived: store versions are immutable, so the history already exists. Same day, same thing? Amend your existing line instead of adding another.

## YYYY-MM-DD

- Hub created from the `project-template` community skill; setup in progress (see `SETUP.md`)

---

Entries older than the window are dropped; read older versions of this skill to see them.